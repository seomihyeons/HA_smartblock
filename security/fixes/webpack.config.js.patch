--- a/webpack.config.js
+++ b/webpack.config.js
@@ -38,10 +38,12 @@ const config = {
     // This will output the same index page with the bundle we
     // created above added in a script tag.
     new HtmlWebpackPlugin({
       template: 'src/index.html',
     }),
-    new webpack.DefinePlugin({
-      __HA_BASE_URL__: JSON.stringify(process.env.HA_BASE_URL),
-      __HA_TOKEN__: JSON.stringify(process.env.HA_TOKEN),
-    }),
+    // SECURITY FIX: Removed hardcoded tokens
+    // Tokens should NEVER be embedded in client-side code
+    // Instead, use a backend proxy server with session authentication
+    // new webpack.DefinePlugin({
+    //   __HA_BASE_URL__: JSON.stringify(process.env.HA_BASE_URL),
+    //   __HA_TOKEN__: JSON.stringify(process.env.HA_TOKEN),
+    // }),
   ],
 };
