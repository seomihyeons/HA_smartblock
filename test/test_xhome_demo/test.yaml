alias: 현관 감지 시 무드램프 켜기
mode: single

trigger:
  - platform: state
    entity_id: binary_sensor.pir_entrance
    to: "on"

condition: []

action:
  - service: light.turn_on
    target:
      entity_id: light.mood_lamp